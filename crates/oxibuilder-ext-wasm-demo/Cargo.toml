[package]
name = "oxibuilder-ext-wasm-demo"
version = "0.9.0"
edition = "2024"
description = "WASM 데모 확장 (doc/08 §8.4). 코어 모듈 + 수동 ABI. oxibuilder-wasm 호스트가 적재한다."
publish = false

[lib]
crate-type = ["cdylib"]
path = "src/lib.rs"
